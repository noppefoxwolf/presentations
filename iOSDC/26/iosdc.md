# イントロダクション

## 自己紹介

こんにちは、Pocochaのデザインエンジニアのnoppeです。

## 目的説明

本トークでは、アプリのUI。特にサイジングについて詳しく解説します。
画面サイズの異なる環境下でも破綻しないUIを実装・設計できるようになる方法を紹介します。
このトークでは、ステップごとに例を示しながら、多くの環境で活躍するAIチャットアプリを作成します。

---

UIKitやSwiftUIを使うと、少ないコードで柔軟なUIのアプリを実装することができるので、アプリの活躍の幅が増えます。
ユーザーは、色々な状況でアプリを使います。
車を運転している時、料理をしている時、ソファでくつろいでいる時、テレビを見ている時。
必ずしも近くにiPhoneがあるとも限りません。
だから、なるべく多くのプラットフォームにアプリを出したいですよね。

---

今回作るAIチャットは、iPhone・iPad、そしてiPhoneミラーリングのリサイズに対応しています。
また、ウィジェットやWatchのコンプリケーションもあります。
// アプリの詳細
このアプリを作ってみましょう。

---

## アジェンダ

柔軟なUIを作るには、いくつかのステップがあります。
まずは、UIのモデリングです。
Figmaの画面デザインから、UIをビューとモデルに分離する方法を紹介します。
次に、サイジングやレイアウトを使用して、ビューが連続的なリサイズに対応できるようにします。
次に、形状変化を考慮して、ユーザーのユースケースに最適化します。
仕上げに、APIデザインを整理して、UIコンポーネントを抽象化します。

---

# UIモデリング

UIがどんなモデルで構成されているかを知ることは、柔軟なUIを作る上で一番大切なことです。
UIは、全ての見た目で別々の情報を表示しているのではなく、一貫して何を表現しています。
そして、その情報というのはUIデザインの過程で無意識に扱われていることも多く、実際の現場ではプロトタイピングやビジュアル表現が先行し、絵としてのUIから実装を開始する事も少なくありません。
そんな時に役立つのが、UIのリバースエンジニアリングです。

---

まず、UIデザインを見てみましょう。
よくあるアプリのUIです。
UIは次の３つに分けることができます。

A.ビジュアルデザイン
	画面に何が描かれているか

B.モデルとユースケース
	何を表現し、ユーザーが何を行うか

C.ビューとレイアウト
	それを現在の画面サイズにどう配置するか

例えば、このUIは次のようなモデルに分離できるはずです。
このように、リバースエンジニアリングをすればビジュアルからモデルを抽出することができます。
抽出には少し経験が必要ですが、LLMを使ってモデルを抽出することもできます。
// 観点をいくつか
1. レイアウトや装飾情報を含まない

PPCなどの画像が読めるLLMに、渡せばすぐに結果を得ることができます。

---

この３つの構造のうち、実行される環境や状況に合わせてビューとレイアウトやビジュアルデザインが決定するように設計をします。
この２つを変更しても、ユースケースは変わらないため、ユーザーは目的を果たすことができます。

そして、ここで大事な原則を覚えておきましょう。
それは「デザインデータは仕様書ではない」ということです。
ビューとレイアウトやビジュアルデザインは、環境の影響を受けます。
つまり、デザインデータとは、「特定の環境」における最終的なアウトプットであり、それ自体を仕様にすることはできないわけです。
10年くらい前はピクセルパーフェクトな実装にするために、トレースする文化もありましたが、現代ではバッドプラクティスと言えるでしょう。
今では、アプリの柔軟性が必要になってきた。
環境とは、アクセシビリティやハードウェアや機能の多様性。
UIデザインを行う人間も、この原則を理解して作らなくてはいけません。

---

では、これらをもとに簡単な例から始めてみましょう。

---

# サイジング


## 静的なデザイン実装の問題点

普段どのようにUI実装をしているか、figmaをそのまま再現しようとしていないか？
デザインデータというのは、本来ある「デザインの概念」の特定の状態の写像でしかない。静的なもの。


## 動的なデザイン実装とは

天気アプリの例
多くの場所で同じアプリが活躍している
こういったものを作れるようになりたい
// Ultraの話題も入れる

# 基礎的なサイジング対応

## セルフサイジング

まず、柔軟性の無いUIとは何だろうか。分かりやすいのはfixedなサイズを持ったビューだ。
ビューは基本的に、自身のコンテンツが主となりサイズが決定する。
そうする事で、自身のコンテンツを全て画面に表示する事が出来る。

## スクロール

## グリッド

## 拡張

safeArea
backgroundExtension
センタリングして余白追加

# 動的形状変化コンポーネント

モデルを共有すれば、ビューは複数のバリエーションを持つことができます。

## タブビュー

# カスタムビューの実装

## 設計

### アンチパターン

割合で実装すること、landscapeにしたらおかしいのは分かる

## 実装

形状ごとにコンポーネントを作る。
モデルを共通化する。
レイアウト切り替えの層を設ける。
SwiftUIで作るのがいい。アニメーションが楽で再レイアウトの処理が得意なので

# まとめ

## マインドセット

UIに柔軟性を持たせた設計をする事で、多様な環境で活躍するアプリにする事が出来る。
そして、それをやるのはもはやデザイナーではなく、プラットフォームのスペシャリストである我々がやる仕事である。

## 柔軟性のあるUIで構築されたアプリの実装

画面サイズなどに依存せず、コンポーネントだけが羅列される。

## 組織論

組織論の話

最後にまとめでチェックリスト。


# 他に話したい話

HIGでは、Flexibilityという言葉。
ビューの見た目はコンテナと環境とモデルによって決定する
plainリストはリサイズに弱いのでgroupedにしたほうがいい。SiriAIとか
