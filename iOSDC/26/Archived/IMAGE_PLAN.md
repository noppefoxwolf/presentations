# iOSDC 2026 画像計画

`iosdc2.md` の画像TODOを差し替えるための補助資料。画像はまだ生成・追加していない。

## 1. iPad／横向きの余白

1. テーマ／スライド：iPhone向けUIを広い画面で開いたときの余白と低い情報密度。
2. 比較する状態：iPhone縦向きの自然な表示と、iPadまたは横向きウインドウで余白が増えた表示。
3. 必須要素：同じニュースカード、広い空白、画面サイズの違いが分かる外枠。
4. 伝えたい一文：「広くなったのに情報量が増えないと、画面はまばらになる。」
5. Deckset配置：全面または右側。

## 2. 中央配置と複数カラム

1. テーマ／スライド：広い画面への複数の正当な適応方法。
2. 比較する状態：読みやすい最大幅で中央配置した状態と、複数カラム／補助情報を並べた状態。
3. 必須要素：同じ記事、コンテンツの最大幅、左右余白、複数列、一覧と詳細の関係。
4. 伝えたい一文：「どちらを選ぶかは、コンテンツとユーザーの目的で決まる。」
5. Deckset配置：左右のサイド・バイ・サイド。

## 3. 可変グリッド

1. テーマ／スライド：記事カードを利用可能幅に適応させるグリッド。
2. 比較する状態：狭い幅の1列と、広い幅の複数列。
3. 必須要素：同じ記事カード、カードの最小幅、列数の変化、カードが許容範囲で広がる様子。
4. 伝えたい一文：「最小カード幅を保ちながら、使える幅から列数を決める。」
5. Deckset配置：右側または全面。

## 4. ViewThatFitsの縦／横レイアウト

1. テーマ／スライド：同じ記事カードの内部配置を幅に応じて変える例。
2. 比較する状態：アイコンとタイトルを横に並べた状態、縦に並べた状態。
3. 必須要素：同じ記事カード、アイコン、タイトル、横配置と縦配置の対応する意味。
4. 伝えたい一文：「意味は同じまま、利用可能幅に合う配置を選ぶ。」
5. Deckset配置：右側。

## 5. タブバーとサイドバー

1. テーマ／スライド：狭い／広いウインドウでの構成・モード変更。
2. 比較する状態：狭いウインドウのタブバーと、広いウインドウのサイドバー＋一覧・詳細。
3. 必須要素：同じニュース選択の目的、タブバー、サイドバー、記事一覧、記事詳細、幅の違い。
4. 伝えたい一文：「構成は変わっても、ユーザーの目的は変わらない。」
5. Deckset配置：左右のサイド・バイ・サイド。

## 6. Article系モデルからWatchの新着件数まで

1. テーマ／スライド：同じモデルを異なるコンテキストへ投影する情報表現の違い。
2. 比較する状態：広い文脈でArticleList・Article・ArticleSourceを同時に扱う状態、iPhoneの一覧→詳細、Watchアプリの1記事／読み上げ、コンプリケーションの新着件数。
3. 必須要素：Article／ArticleList／ArticleSourceの関係、iPhoneの選択と詳細、Watchの件数、モデルは同じで表現だけが変わること。
4. 伝えたい一文：「モデルとユースケースを保ったまま、必要な情報を環境に合わせて投影する。」
5. Deckset配置：全面または横並び。
